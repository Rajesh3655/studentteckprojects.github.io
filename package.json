{
  "name": "studenttechprojects-astro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:details": "node scripts/generate-detail-pages.js",
    "build:sitemap": "node scripts/generate-sitemap.js",
    "build:static": "npm run build:details && npm run build:sitemap"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.2.0"
  }
}
